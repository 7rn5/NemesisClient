package nemesis.impl.module.misc;

import nemesis.impl.module.Module;

public class SplashText extends Module {
    public SplashText() {
        super("SplashText", "Change the splash text in the main menu to the client's original version", Category.Misc);
    }
}